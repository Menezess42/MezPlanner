from flask import render_template, Blueprint

todos = Blueprint(
    "todos", __name__, template_folder="templates", static_folder="static"
)


@todos.route("/")
def index():
    return render_template("todos/index.html")
